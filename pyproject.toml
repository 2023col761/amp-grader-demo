[project]
name = "amp-grader-demo"
version = "0.1.0"
description = "AMP Challenge assignment starting point: datasets, assignment doc, and a runnable copy of the real grading pipeline"
requires-python = ">=3.11"
dependencies = [
    "seqme[esm2,aa_descriptors]>=0.5.1",
    "numpy>=2.2.6",
]

[tool.uv]
package = false
